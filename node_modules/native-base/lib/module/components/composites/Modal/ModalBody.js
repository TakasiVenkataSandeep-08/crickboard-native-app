import React from 'react';
import { Box } from '../../primitives';
import { useThemeProps } from '../../../hooks';

const ModalBody = props => {
  // const { newProps } = React.useContext(ModalContext);
  const newProps = useThemeProps('ModalBody', props);
  return /*#__PURE__*/React.createElement(Box, newProps);
};

export default /*#__PURE__*/React.memo(ModalBody);
//# sourceMappingURL=ModalBody.js.map