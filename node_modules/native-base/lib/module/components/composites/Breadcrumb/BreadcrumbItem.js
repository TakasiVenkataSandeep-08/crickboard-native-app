import React from 'react';
import Flex from '../../primitives/Flex';

const BreadcrumbItem = props => {
  const {
    children,
    isCurrentPage,
    _text,
    ...remainingProps
  } = props;
  return /*#__PURE__*/React.createElement(Flex, remainingProps, React.Children.map(children, (child, index) => /*#__PURE__*/React.cloneElement(child, {
    key: "breadcrumb-item-".concat(index),
    _text: { ..._text,
      //taken out empty _text prop from props
      fontWeight: isCurrentPage ? 'bold' : undefined
    },
    ...{
      isUnderlined: false
    },
    ...remainingProps
  })));
};

export default /*#__PURE__*/React.memo(BreadcrumbItem);
//# sourceMappingURL=BreadcrumbItem.js.map