import type { ColorMode, StorageManager } from './types';
export declare const useColorMode: () => import("./types").IColorModeContextProps;
export declare function useColorModeValue(light: any, dark: any): any;
export declare function useModeManager(initialColorMode: ColorMode, colorModeManager?: StorageManager): {
    colorMode: ColorMode;
    setColorMode: (val: ColorMode) => Promise<void>;
};
