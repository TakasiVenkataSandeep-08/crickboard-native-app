import { Animated } from 'react-native';
export declare const useFadeTransition: (duration?: number, initValue?: number, finalValue?: number) => {
    fadeValue: Animated.Value;
    fadeIn: () => void;
    fadeOut: () => void;
};
