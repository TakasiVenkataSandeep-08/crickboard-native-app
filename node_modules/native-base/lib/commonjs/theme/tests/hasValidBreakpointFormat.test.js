"use strict";

var _utils = require("./../../theme/tools/utils");

describe('mode', () => {
  test('Empty array', () => {
    expect((0, _utils.hasValidBreakpointFormat)([])).toBe(false);
  });
  test('Array', () => {
    expect((0, _utils.hasValidBreakpointFormat)([1, 2])).toBe(true);
  });
  test('Valid Object', () => {
    expect((0, _utils.hasValidBreakpointFormat)({
      base: 1,
      sm: 2
    })).toBe(true);
  });
  test('Invalid Object', () => {
    expect((0, _utils.hasValidBreakpointFormat)({
      base: 1,
      sm: 2,
      ab: 1
    })).toBe(false);
  });
});
//# sourceMappingURL=hasValidBreakpointFormat.test.js.map